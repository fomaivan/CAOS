#include <stdio.h>
#include <string.h>

// solution.c
int main() { 
  printf("%s", string);
  int result = 0;
  int arr[] = number;
  int size = sizeof(arr) / sizeof(int);
  for (int i = 0; i < size; i++) {
    result += arr[i];
  }
  printf("%d\n", result);
  return 0;
}
